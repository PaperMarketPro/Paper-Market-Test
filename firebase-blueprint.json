{
  "entities": {
    "users": {
      "title": "User Document",
      "description": "User specific data, paper trading positions, order logs, learning progress, AI journals, and active strategy setups.",
      "type": "object",
      "properties": {
        "userProfile": { "type": "object", "description": "Profile info like name, experience, virtual balance, streak, and level." },
        "orders": { "type": "array", "description": "Historic and pending paper trade logs." },
        "positions": { "type": "array", "description": "Currently active or closed paper trading positions." },
        "journals": { "type": "array", "description": "AI-generated psychological journal entries analyzing trades." },
        "strategies": { "type": "array", "description": "Saved automation rules and indicators for the Strategy Builder." },
        "cognitiveRules": { "type": "array", "description": "Rules generated by AI Coach to optimize psychology." },
        "courses": { "type": "array", "description": "Course learning suite completion metrics." },
        "challenges": { "type": "array", "description": "Active gamified trading challenges progress." },
        "badges": { "type": "array", "description": "Earned reward badges." },
        "notifications": { "type": "array", "description": "User alerts and risk notices." }
      }
    }
  },
  "firestore": {
    "users/{userId}": {
      "schema": "users",
      "description": "Contains user-specific documents."
    }
  }
}
